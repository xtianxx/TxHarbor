# 008/009 并行开发工作流规则（决策记录，2026-09-15）

范围：仅调整 008 nonce-manager 与 009 signer-service 的并行工作流规则并记录决策。
不启动 specify，不做业务实现，不修改章程与业务代码。

## R1 串行规则的限定例外

既有严格串行规则增加一条限定例外：008 与 009 可在**共同契约确定、
各自完成适用规划（plan）后**并行开发。合并验收顺序仍为 008 → 009。
010、011 不纳入本次例外，仍严格串行。

## R2 一次只推进一个 Spec Kit 工作流步骤

保留该约定。后续若同一次要对两个阶段执行同类步骤，必须在该次指令中
明确范围（例如“仅 008 的 plan”或“008 + 009 的 clarify”），不自动跨入下一步骤。

## R3 共同契约待确定事项（OPEN，本次未裁决）

以下事项只记录为待确定，不得在本次流程调整中自行批准业务答案，
留待后续 clarify / specify 裁决：

1. 付款意图创建与身份
2. sender 权威来源
3. nonce 持久绑定
4. 签名请求与交易尝试身份
5. 执行授权
6. 006 恢复暂停
7. 检查到副作用之间的竞争

## R4 实现组织方式

- 008 / 009 分别使用独立工作目录和独立分支，由同一个主编排统筹后台任务，
  不固定子模型。
- 共享接口、迁移和公共文件的每次修改必须明确归属（同一文件在同一步骤内
  只由一个阶段写入）。
- 并行工作目录不等于测试资源隔离：`compose.yaml` 的 PostgreSQL / Anvil
  端口与数据卷为单套共享，数据库、端口及其他本地测试资源也须协调。

## R5 009 可独立开发的范围

009 可以独立开发已确定契约下的进程隔离、认证、交易校验和签名核心。
真实 nonce / 意图绑定及恢复暂停接线必须等待相应依赖可用。
测试替身不能替代最终集成验收（章程 XI： mocks 不得替代并发/恢复类集成验证）。

## R6 单一活动 feature 状态与分别定位

- 检查结果：增强 Spec Kit 只存在单一活动 feature 状态，即
  `.specify/feature.json`（当前指向 `specs/007-withdrawal-creation`）；
  每次 specify 会覆盖它，下游命令默认以它定位。
  另注意 `before_specify` hook 为强制启用，会自动创建/切换分支，
  须与 R4 的独立分支策略协调。
- 后续 008 / 009 分别定位：任何步骤必须显式声明并使用各自的
  `SPECIFY_FEATURE_DIRECTORY`（`specs/008-*` / `specs/009-*`），
  不得默认依赖 `feature.json` 的当前值；主编排或子任务在读写前先声明目标目录，
  不得读取、覆盖另一阶段的产物。
- 本次不创建工作目录、不启动后台实现。

## R7 保留的既有节奏

保留批次检查（build、lint、unit tests 含 race、integration tests）、
本地提交、报告后停止的节奏，以及 T000-P 独立 open。
`requirements` 按真实状态报告。`M .gitignore` 与 `?? .omo/` 为既有本地状态，
保留排除，不纳入提交。

## 本次实际工作流限制

- 不升级 OMO，不运行 specify。
- 不推送、不创建 PR、不合并、不部署。
- 仅新增本文档与 `docs/project-context.md`，本地提交后停止。
